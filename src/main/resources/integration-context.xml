<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:gateway id="fuckingGateway"
                 service-interface="com.oci.springintegration.FuckingGateway"
                 default-request-channel="channelOne"/>

    <int:channel id="channelOne"/>
    <int:channel id="channelTwo"/>
    <int:channel id="channelThree"/>

    <!--    <int:converter ref="errorChannel" />-->

    <int:chain input-channel="channelOne">
        <int:filter ref="fuckingFilter"/>
        <int:filter ref="motherFuckingFilter"/>
        <int:enricher>
            <int:property name="hello" expression="headers['hola']"/>
        </int:enricher>
        <int:service-activator ref="fuckingService"/>
    </int:chain>

</beans>
